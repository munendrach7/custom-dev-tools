{
  "name": "custom-dev-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Custom Dev Tools — a dockerized workspace hosting a JSON/JSONL viewer and a Postman-like API client. Monorepo root / dev orchestrator.",
  "scripts": {
    "dev": "concurrently -k -n JSONL,API,WEB -c green,magenta,cyan \"npm:dev:jsonl\" \"npm:dev:api\" \"npm:dev:web\"",
    "dev:web": "npm --prefix frontend run dev",
    "dev:jsonl": "npm --prefix services/jsonl-api start",
    "dev:api": "cd services/api-client && python -m uvicorn app.main:app --reload --port 8000",
    "build": "npm --prefix frontend run build",
    "install:all": "npm --prefix frontend install && npm --prefix services/jsonl-api install"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
